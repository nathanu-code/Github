{
  "name": "lead-capture-funnel",
  "version": "1.0.0",
  "private": true,
  "description": "Two-page application funnel: landing page, 8-step qualifying quiz with partial capture, booking.",
  "scripts": {
    "start": "node test/server.js",
    "build": "node build.js",
    "test": "node test/e2e.js"
  },
  "devDependencies": {
    "playwright-core": "^1.56.1"
  }
}
